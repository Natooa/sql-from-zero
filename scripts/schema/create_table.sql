CREATE TABLE students(
                         id SERIAL PRIMARY KEY,
                         name VARCHAR(50),
                         surname VARCHAR(50),
                         course INT,
                         email VARCHAR(100)
);